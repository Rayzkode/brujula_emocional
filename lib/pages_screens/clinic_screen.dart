import 'package:flutter/material.dart';

class clinic_page extends StatelessWidget {
  const clinic_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
