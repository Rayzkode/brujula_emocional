/*Esta pantalla te debe dar la opcion como usuario de poder asignarle un
pint contaseña a la app al momento de iniciarla, aqui realmente no se como orientarte,
puedes buscar ejemplos ya sea en apps o en internet*/

import 'package:flutter/material.dart';

class PinScreen extends StatelessWidget {
  const PinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
